import Comingsoon from "../Comingsoon";

function StopwatchPage({ isOpen, toggleSidebar }) {
  return (
    <Comingsoon
      name="StopwatchPage Coming soon"
      isOpen={isOpen}
      toggleSidebar={toggleSidebar}
    />
  );
}

export default StopwatchPage;
