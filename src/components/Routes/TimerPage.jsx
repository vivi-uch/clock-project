import Comingsoon from "../Comingsoon";

function TimerPage({ isOpen, toggleSidebar }) {
  return (
    <Comingsoon
      name="TimerPage Coming Soon!!!"
      isOpen={isOpen}
      toggleSidebar={toggleSidebar}
    />
  );
}

export default TimerPage;
