import Comingsoon from "../Comingsoon";

function WorldClockPage({ isOpen, toggleSidebar }) {
  return (
    <Comingsoon
      name="WorldClockPage Coming Soon!!!"
      isOpen={isOpen}
      toggleSidebar={toggleSidebar}
    />
  );
}

export default WorldClockPage;
