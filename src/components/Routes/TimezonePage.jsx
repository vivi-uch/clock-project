import Comingsoon from "../Comingsoon";

function TimezonePage({ isOpen, toggleSidebar }) {
  return (
    <Comingsoon
      name="TimezonePage Coming Soon!!!"
      isOpen={isOpen}
      toggleSidebar={toggleSidebar}
    />
  );
}

export default TimezonePage;
